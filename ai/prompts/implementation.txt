For the following remediation item, generate deployable Azure implementation components aligned to the official ALZ design areas.

The item includes an architectural decision (selected_pattern, alz_module) from the
Implementation Decision pass. Use that selection — do NOT override it with a different pattern.

CRITICAL: The checklist_id comes from the ALZ review-checklists (e.g. A01.01, B02.03).
Do NOT generate synthetic IDs like INIT-001.

Official ALZ Design Areas:
  1. Azure Billing and Microsoft Entra ID Tenants
  2. Identity and Access Management
  3. Network Topology and Connectivity
  4. Security
  5. Management
  6. Resource Organization
  7. Platform Automation and DevOps
  8. Governance

Return ONLY valid JSON matching this schema:

{
  "checklist_id": "A01.01",
  "selected_pattern": "The pattern name from the implementation decision",
  "alz_module": "The ALZ Bicep module from the implementation decision",
  "prerequisite_items": ["checklist_ids this depends on from the sequence justification"],
  "implementation": {
    "bicep_modules": [
      {
        "module_name": "...",
        "alz_module_ref": "Reference to ALZ Bicep module if applicable",
        "description": "...",
        "parameters": ["..."],
        "target_scope": "managementGroup | subscription | resourceGroup"
      }
    ],
    "policy_assignments": [
      {
        "policy_definition": "...",
        "display_name": "...",
        "scope": "...",
        "parameters": {}
      }
    ],
    "rbac_roles": [
      {
        "role": "...",
        "scope": "...",
        "justification": "..."
      }
    ],
    "pipelines": [
      {
        "name": "...",
        "trigger": "...",
        "steps": ["..."]
      }
    ],
    "validation_queries": [
      {
        "name": "...",
        "language": "KQL | ARG",
        "query": "..."
      }
    ]
  }
}

Rules:
- Use real Azure resource types and built-in policy definition IDs where possible
- Bicep modules should target the correct ARM scope (MG, sub, or RG)
- Policy assignments must reference real built-in or well-known custom definitions
- RBAC roles must use official Azure role names
- Validation queries should be Azure Resource Graph (ARG) or KQL that can verify success
- Pipelines should be GitHub Actions or Azure DevOps YAML compatible
- Match implementation scope to the item's delivery_model.change_scope

Item:
{{ITEM}}